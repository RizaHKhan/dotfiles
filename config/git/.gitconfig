[user]
	email = khanriza@gmail.com
	name = Riza Khan

[init]
  defaultBranch = main

[core]
	excludesfile = ~/.gitignore_global
  autocrlf = input

[pager]
  difftool = true

[color]
	# Use colors in Git commands that are capable of colored output when
	# outputting to the terminal. (This is the default setting in Git ≥ 1.8.4.)
	ui = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold # line info
	old = red # deletions
	new = green # additions

[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[diff]
	# Detect copies as well as renames
  renames = copies
  external = difft
  tool = difftastic

[gpg]
	format = ssh
[gpg "ssh"]
	program = /opt/homebrew/bin/op-ssh-sign
[commit]
	gpgsign = true

[include]
	path = ~/.gitconfig.local

[alias]
    cdiff = !nvim \"+CodeDiff --exit-on-close\"
    # Difftastic aliases
    dlog = -c diff.external=difft log --ext-diff
    dshow = -c diff.external=difft show --ext-diff
    ddiff = -c diff.external=difft diff
    # `git log` with patches shown with difftastic
    dl = -c diff.external=difft log -p --ext-diff
    # Show the most recent commit with difftastic
    ds = -c diff.external=difft show --ext-diff
    # `git diff` with difftastic
    dft = -c diff.external=difft diff

[help]
	# Automatically correct and execute mistyped commands
	autocorrect = 1

[merge]
	# Include summaries of merged commits in newly created merge commit messages
	log = true

[commit]
	template = /Users/emrearmagan/.stCommitMsg

[pull]
	rebase = true

[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f

[credential]
	helper = cache

[merge]
  tool = codediff

[mergetool "codediff"]
  cmd = nvim -f -c "CodeDiff" "$MERGED"
  trustExitCode = false
  keepBackup = false

[mergetool "sourcetree"]
	cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
  keepBackup = false
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/

[advice]
	statusHints = false
[includeIf "gitdir:/Users/rkhan/camcloud/"]
	path = /Users/rkhan/.gitconfig-bitbucket
